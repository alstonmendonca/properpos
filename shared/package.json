{
  "name": "@properpos/shared",
  "version": "1.0.0",
  "description": "Shared types, utilities, and constants for ProperPOS SaaS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "jest",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jest": "^29.6.0",
    "typescript": "^5.1.6"
  },
  "files": [
    "dist/**/*"
  ]
}